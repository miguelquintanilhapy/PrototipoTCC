<UserControl x:Class="magal.Views.HistoricoView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="900" d:DesignWidth="1200"
             Background="#F0F4FA">

    <UserControl.Resources>
        <SolidColorBrush x:Key="AeroPrimary"    Color="#1E3A8A"/>
        <SolidColorBrush x:Key="AeroAccent"     Color="#3B82F6"/>
        <SolidColorBrush x:Key="AeroDanger"     Color="#EF4444"/>
        <SolidColorBrush x:Key="AeroTextDark"   Color="#0F172A"/>
        <SolidColorBrush x:Key="AeroTextMuted"  Color="#64748B"/>
        <SolidColorBrush x:Key="AeroTextHint"   Color="#94A3B8"/>
        <SolidColorBrush x:Key="AeroBorder"     Color="#E2E8F0"/>

        <Style x:Key="PrimaryBtn" TargetType="Button">
            <Setter Property="Background"   Value="{StaticResource AeroPrimary}"/>
            <Setter Property="Foreground"   Value="White"/>
            <Setter Property="FontWeight"   Value="Bold"/>
            <Setter Property="FontSize"     Value="11"/>
            <Setter Property="Cursor"       Value="Hand"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Bd" Background="{TemplateBinding Background}" CornerRadius="8" Padding="14,7">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="#1e40af"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Grid Margin="32,28">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Height="80" Margin="0,0,0,10">
            <StackPanel VerticalAlignment="Center">
                <TextBlock Text="Histórico de Projetos" 
                           FontSize="28" 
                           FontWeight="Bold"
                           Foreground="{StaticResource AeroTextDark}"/>
            </StackPanel>

            <Button Content="ATUALIZAR LISTA" 
                    Command="{Binding AtualizarCommand}"
                    Style="{StaticResource PrimaryBtn}"
                    HorizontalAlignment="Right" 
                    VerticalAlignment="Center" 
                    Padding="15,8"/>
        </Grid>

        <UniformGrid Grid.Row="1" Columns="3" Margin="0,0,0,20">
            <Border Background="White" CornerRadius="12" Margin="0,0,10,0" Padding="20,15"
                    BorderThickness="1" BorderBrush="{StaticResource AeroBorder}">
                <StackPanel>
                    <TextBlock Text="PROJETOS TOTAIS" FontSize="10" FontWeight="Bold" Foreground="{StaticResource AeroTextHint}" Margin="0,0,0,5"/>
                    <TextBlock Text="{Binding QuantidadeProjetos}" FontSize="24" FontWeight="Bold" Foreground="{StaticResource AeroPrimary}"/>
                </StackPanel>
            </Border>

            <Border Background="White" CornerRadius="12" Margin="5,0" Padding="20,15"
                    BorderThickness="1" BorderBrush="{StaticResource AeroBorder}">
                <StackPanel>
                    <TextBlock Text="VALOR TOTAL EM PROPOSTAS" FontSize="10" FontWeight="Bold" Foreground="{StaticResource AeroTextHint}" Margin="0,0,0,5"/>
                    <TextBlock Text="{Binding TotalFinanceiro}" FontSize="24" FontWeight="Bold" Foreground="{StaticResource AeroPrimary}"/>
                </StackPanel>
            </Border>

            <Border Background="{StaticResource AeroPrimary}" CornerRadius="12" Margin="10,0,0,0" Padding="20,15">
                <StackPanel>
                    <TextBlock Text="STATUS DO SISTEMA" FontSize="10" FontWeight="Bold" Foreground="#93C5FD" Margin="0,0,0,5"/>
                    <TextBlock Text="AERO v2.0" FontSize="24" FontWeight="Bold" Foreground="White"/>
                </StackPanel>
            </Border>
        </UniformGrid>

        <StackPanel Grid.Row="2" Margin="0,0,0,15">
            <TextBlock Text="PESQUISAR PROJETO OU STATUS" 
                       FontSize="10" FontWeight="Bold" 
                       Foreground="{StaticResource AeroTextHint}" 
                       Margin="0,0,0,5"/>
            <Grid>
                <TextBox Text="{Binding FiltroTexto, UpdateSourceTrigger=PropertyChanged}"
                         Height="38"
                         VerticalContentAlignment="Center"
                         Padding="12,0,40,0"
                         FontSize="14"
                         Background="White"
                         BorderThickness="1"
                         BorderBrush="{StaticResource AeroBorder}">
                    <TextBox.Resources>
                        <Style TargetType="{x:Type Border}">
                            <Setter Property="CornerRadius" Value="8"/>
                        </Style>
                    </TextBox.Resources>
                </TextBox>
                <TextBlock Text="🔍" HorizontalAlignment="Right" VerticalAlignment="Center" 
                           Margin="0,0,12,0" Foreground="{StaticResource AeroTextHint}"/>
            </Grid>
        </StackPanel>

        <Border Grid.Row="3" Background="White" CornerRadius="16" 
                BorderThickness="1" BorderBrush="{StaticResource AeroBorder}" Padding="1">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <DataGrid ItemsSource="{Binding ProjetosView}" 
                          SelectedItem="{Binding ProjetoSelecionado}"
                          AutoGenerateColumns="False" 
                          IsReadOnly="True"
                          Background="Transparent"
                          BorderThickness="0"
                          GridLinesVisibility="Horizontal"
                          HorizontalGridLinesBrush="#F1F5F9"
                          HeadersVisibility="Column"
                          FontSize="13"
                          RowHeight="45">

                    <DataGrid.Resources>
                        <Style TargetType="DataGridColumnHeader">
                            <Setter Property="Background" Value="#F8FAFC"/>
                            <Setter Property="Foreground" Value="{StaticResource AeroTextMuted}"/>
                            <Setter Property="FontWeight" Value="SemiBold"/>
                            <Setter Property="Padding" Value="15,10"/>
                            <Setter Property="BorderThickness" Value="0,0,0,1"/>
                            <Setter Property="BorderBrush" Value="{StaticResource AeroBorder}"/>
                        </Style>
                        <Style TargetType="DataGridCell">
                            <Setter Property="Padding" Value="15,0"/>
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="DataGridCell">
                                        <Border Padding="{TemplateBinding Padding}" Background="{TemplateBinding Background}">
                                            <ContentPresenter VerticalAlignment="Center"/>
                                        </Border>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                            <Style.Triggers>
                                <Trigger Property="IsSelected" Value="True">
                                    <Setter Property="Background" Value="#EFF6FF"/>
                                    <Setter Property="Foreground" Value="{StaticResource AeroPrimary}"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </DataGrid.Resources>

                    <DataGrid.Columns>
                        <DataGridTextColumn Header="DATA" Binding="{Binding data_criacao, StringFormat=dd/MM/yyyy}" Width="120"/>
                        <DataGridTextColumn Header="NOME DO PROJETO" Binding="{Binding nome}" Width="*"/>
                        <DataGridTextColumn Header="TIPO" Binding="{Binding tipo}" Width="150"/>
                        <DataGridTextColumn Header="STATUS" Binding="{Binding status}" Width="120"/>

                        <DataGridTemplateColumn Header="AÇÕES" Width="180">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <StackPanel Orientation="Horizontal">
                                        <Button Content="Editar" 
                                                Command="{Binding DataContext.EditarCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                CommandParameter="{Binding}"
                                                Margin="0,0,8,0" Foreground="{StaticResource AeroAccent}" 
                                                Background="Transparent" BorderThickness="0" Cursor="Hand" FontWeight="Bold"/>
                                        <Button Content="Excluir" 
                                                Command="{Binding DataContext.ExcluirCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                CommandParameter="{Binding}"
                                                Foreground="{StaticResource AeroDanger}" 
                                                Background="Transparent" BorderThickness="0" Cursor="Hand" FontWeight="Bold"/>
                                    </StackPanel>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                    </DataGrid.Columns>
                </DataGrid>

                <Border Grid.Row="1" Background="#F8FAFC" BorderThickness="0,1,0,0"
                        BorderBrush="#F1F5F9" CornerRadius="0,0,16,16" Padding="20,12">
                    <TextBlock Text="{Binding Projetos.Count, StringFormat='{}{0} projetos encontrados'}"
                               FontSize="12" Foreground="{StaticResource AeroTextHint}" VerticalAlignment="Center"/>
                </Border>
            </Grid>
        </Border>
    </Grid>
</UserControl>